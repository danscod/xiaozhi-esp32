tmp/
components/
managed_components/
build/
dist/
.vscode/
.devcontainer/
sdkconfig.old
sdkconfig
dependencies.lock
.env
releases/
main/assets/lang_config.h
main/mmap_generate_emoji.h
.DS_Store
.cache
*.pyc
*.bin
mmap_generate_*.h
.clangd

# Host/IDE manager config (machine-specific Windows paths)
eim_config.toml
# Local test media (large; not for the repo)
videos/
# DOOM WAD — build input baked into the assets partition, kept local
# like components/ (the prboom engine). Provide doom1.wad locally to build.
*.wad
